# portal
```bash
# 查找vite可以使用的版本，由于7.x版本要求node版本比较高
# 本机系统node@v20.18.0依旧被提示版本过低。
npm view create-vite versions 

# 使用vite v6最后版本
npm create vite@v6.5.0

# 安装react-router
npm i react-router

# 安装bootstrap 和 react-bootstrap
npm install react-bootstrap bootstrap

# 一次性安装常用工具
npm i clsx zustand dayjs axios radash
```